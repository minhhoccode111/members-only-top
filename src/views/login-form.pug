extends layout

block content
  h2(class="text-2xl font-bold")!= title
  div(class="my-8")
    form(method="post") 
      div(class="p-2 m-2")
        label(for="username" class="block text-lg") *Username
        input(class="box-border w-full border-t border-gray-100 outline-none shadow-md shadow-gray-400 px-2 py-1 rounded-md focus-within:shadow-inner focus-within:border focus-within:border-gray-500 peer" id="username" name="username" type="text" placeholder="abc123@gmail.com" required)

      div(class="p-2 m-2")
        label(for="password" class="block text-lg") *Password
        input(class="box-border w-full border-t border-gray-100 outline-none shadow-md shadow-gray-400 px-2 py-1 rounded-md focus-within:shadow-inner focus-within:border focus-within:border-gray-500" id="password" name="password" type="password" placeholder="Your super secret password" required)

      div(class="flex items-center justify-end p-4")
        button(type="submit" class="font-bold text-white bg-green-500 px-4 py-2 rounded-md border border-white hover:border-green-500 hover:text-green-500 hover:bg-white transition-all") Login 
  
  div(class="")
    p Don't have an account?
      a(href="/signup" class="text-sky-500 transition-all hover:text-white hover:bg-sky-500 p-1 rounded underline underline-offset-4 decoration-dotted hover:decoration-solid font-bold") Sign up
      |now.
      
  if messages
    ul
      each mess in messages 
        li!= mess
      
  div(class="text-red-500") 
    p 
      strong *You need to log in to create messages.
    p 
      strong *Anonymous users have read-only access to the site.