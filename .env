# port to run the app
PORT=3000

# production URI, if not specified then use the URI hard-coded in app.js
MONGODB_URI=""

# combine with debug module
DEBUG="custom-debug"

# passcodes, change to something else in production
MEMBERSHIP_PASSCODE="asd"
ADMIN_PASSCODE="asd"

# environment
NODE_ENV="development"
